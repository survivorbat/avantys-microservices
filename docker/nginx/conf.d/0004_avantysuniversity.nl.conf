server {
    listen 8081 ssl default deferred;

    root /srv/html;

    server_name ${server_name};

    location / {
        return 200 "<html><body>This website is work in progress.</body></html>";
    }
}