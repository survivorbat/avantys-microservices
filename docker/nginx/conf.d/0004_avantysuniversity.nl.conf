server {
    listen 8081 ssl default deferred;

    root /srv/html;

    server_name ${server_name};

    location / {
        add_header Content-Type text/plain;
        return 200 "WIP Website";
    }
}